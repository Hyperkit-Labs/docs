# v0.1.0 archived documentation (materialized)

This folder holds the **supporting bundle** for `/v0.1.0/*` routes: components and `lib` copied from the pinned git commit in `lib/archive-v0.1.0-config.ts`.

- **Page sources** live under `app/v0.1.0/` (generated; do not hand-edit without re-running the materializer).
- **To regenerate** after changing the pinned SHA: `npm run materialize:v0.1.0`
